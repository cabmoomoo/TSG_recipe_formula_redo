-- All mods must have a manifest.lua file in their root folder, with the
-- following structure.
return {
    name = "Recipe Redo",
    description = "Changes a formula used for determining recipe products so that they can have multiple of the same type. Changes nothing on the user's end, only required for mods that need it.",
    -- This field is mandatory and for now it has to be 4.
    api_version = 4
}
